{
  "name": "bountyscope",
  "version": "0.1.0",
  "private": true,
  "description": "Bug-bounty intel + on-demand AI smart-contract analysis (Cloudflare Worker).",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "node --test tests/*.test.mjs",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "@eslint/js": "^9.11.0",
    "eslint": "^9.11.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.7.0",
    "vitest": "^4.1.9",
    "wrangler": "^3.78.0"
  }
}
